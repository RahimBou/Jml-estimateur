'use strict';const {loadDvf,consolidate,selectFromBase,TYPES}=require('../server');function d(v){const x=new Date(String(v).slice(0,10));return Number.isNaN(x.getTime())?null:x}function median(a){const x=[...a].sort((a,b)=>a-b);if(!x.length)return 0;const i=(x.length-1)/2;return x[Math.floor(i)]===x[Math.ceil(i)]?x[Math.floor(i)]:(x[Math.floor(i)]+x[Math.ceil(i)])/2}function pct(x){return +(x*100).toFixed(1)}function wm(a){const x=a.sort((a,b)=>a.v-b.v),h=x.reduce((s,z)=>s+z.w,0)/2;let s=0;for(const z of x){s+=z.w;if(s>=h)return z.v}return 0}async function run(type,k){const rows=await loadDvf(),all=consolidate(rows,TYPES[type]),tests=all.filter(r=>d(r.date)?.getFullYear()===2025&&r.area>=30&&r.area<=300&&r.price>=20000).sort((a,b)=>String(a.date).localeCompare(String(b.date)));const sample=tests.filter((_,i)=>i%Math.max(1,Math.floor(tests.length/100))===0).slice(0,100),e=[];for(const sold of sample){const sd=d(sold.date),prior=all.filter(r=>d(r.date)?.getTime()<sd.getTime());if(prior.length<6)continue;const t={lat:sold.lat,lon:sold.lon,area:sold.area,rooms:sold.rooms||0,landArea:sold.landArea||0},c=selectFromBase(prior,t,sd).slice(0,k);if(!c.length)continue;const p=Math.round(wm(c.map(r=>({v:r.sqmPrice,w:r.weight}))) * t.area/1000)*1000;e.push((p-sold.price)/sold.price)}const a=e.map(Math.abs);return{type,k,n:e.length,medianAbs:pct(median(a)),mae:pct(a.reduce((x,y)=>x+y,0)/e.length),within20:pct(a.filter(x=>x<=.2).length/e.length)}}(async()=>{for(const t of ['apartment','house'])for(const k of [5,8,10,12,15])console.log(JSON.stringify(await run(t,k)))})().catch(e=>{console.error(e);process.exit(1)})